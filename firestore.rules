rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regras temporárias para teste - permite acesso total
    // NÃO USE EM PRODUÇÃO
    match /{document=**} {
      allow read, write: if true;
    }
  }
}